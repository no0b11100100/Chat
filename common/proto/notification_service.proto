syntax = "proto3";

package notification;

option go_package = "/;api";
import "google/protobuf/empty.proto";

message Notification {
    string userId=1;
    string message=2;
    string id=3;
}

service NotificationService {
    rpc RecieveNotification(google.protobuf.Empty) returns(stream Notification);
}
